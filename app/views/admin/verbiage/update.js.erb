$.modal.close();
$('#verbiage_<%= @verbiage.title %> .blue_content').html("<%= escape_javascript @verbiage.content %>");
verbiage_updated_callback($('#verbiage_<%= @verbiage.title %> .blue_content'));

$.jGrowl("<%= escape_javascript(flash[:notice]) %>", { header: 'Success', theme:'success' });
