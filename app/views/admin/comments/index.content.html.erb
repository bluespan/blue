<h2>Comments</h2>


<% if not @commentable.comments.empty? %>

<ol class="comments">
	<% @commentable.comments.each do |comment| %>
	<%= render :partial => "comment", :locals => {:comment => comment}%>
	<% end %>
</ol>

<% else %>
<p id="no_comments">No comments have been made yet.</p>
<% end %>

<% form_for @comment, :url => admin_verbiage_comments_path(@commentable, :format => :js) do |f| %>
	
	<label for="comment_comment">Comment</label>
	<%= f.text_area :comment %>
	
	<input type="submit" value="Create" class="button submit" />
<% end %>