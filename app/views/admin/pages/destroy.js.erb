$("#page_<%= @old_page_id %>").fadeOut(function(){ $(this).remove() })
<% @old_navigations.each do |old_navigation| %>
	$("#navigation_<%= old_navigation.id %>").fadeOut(function(){ $(this).remove(); updateBucketPreviews() })
<% end %>
$.jGrowl("<%= escape_javascript(flash[:notice]) %>", { header: 'Success', theme:'success' });