<h1>New Admin User</h1>

<% form_for [:admin, @admin_user] do |f| %>
	<%= render :partial => 'form', :locals => {:f => f} %>
	
	<div class="buttons">
		<input type="submit" value="Create" class="button submit" /> | <a href="#cancel" class="close">Cancel</a>
	</div>
	
<% end %>

<%= render :partial => 'photo_form' %>